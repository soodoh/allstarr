pre-commit:
  commands:
    check:
      glob: "src/**/*.{js,jsx,ts,tsx,css,html,json}"
      staged: true
      run: bunx biome check --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
